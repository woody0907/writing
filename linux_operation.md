### config environment variable
* global user 
    ```
    vi /etc/profile
    evn
    source /etc/profile
    ```
* local user
   ```
   vi ~/.bash_profile
   set 
   unset
   source ~/.bash_profile
   ```
* current shell
    ```
    export XXX=xxx
    ```

